import { Text } from 'react-native';

import { SafeAreaView } from 'react-native-safe-area-context';

import { ChatBumble } from './ChatBumble';

export function ChatContainer() {
  return (
    <SafeAreaView className="gap-6\ w-full flex-1 flex-col">
      
    </SafeAreaView>
  );
}
