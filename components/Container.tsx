import { SafeAreaProvider } from "react-native-safe-area-context";

export function Container() {
    return (
        <SafeAreaProvider className="dark:bg-black">
            
        </SafeAreaProvider>
    );
}